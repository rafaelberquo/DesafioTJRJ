@model DesafioTJRJ.UI.ViewModels.AssuntoViewModel

@{
    ViewData["Title"] = "Visualizar Assunto";
}

<h1>@ViewData["Title"]</h1>
<hr />

<div class="row">
    <div class="col-md-8">
        <div class="form-group">
            <label asp-for="Descricao" class="control-label"></label>
            <input asp-for="Descricao" class="form-control" readonly />
        </div>

        <div class="form-group text-right">
            <a asp-action="Editar" asp-route-id="@Model.CodAs" class="btn btn-warning">Editar</a>
            <a asp-action="Index" class="btn btn-primary">Cancelar</a>
        </div>
    </div>
</div>